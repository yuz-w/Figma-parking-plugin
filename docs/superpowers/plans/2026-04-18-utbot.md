# utbot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Figma에서 프레임을 선택하면 Nielsen 10 휴리스틱 기반 사용성 분석 리포트를 캔버스에 자동 생성하는 플러그인 utbot을 구현한다.

**Architecture:** ui.html(패널)과 code.js(샌드박스)가 postMessage로 통신한다. 규칙 기반 스캔은 code.js에서 Figma 노드 트리를 순회하고, Claude API 호출도 code.js에서 fetch로 수행한다. 분석 결과는 선택 프레임 오른쪽에 새 Figma 프레임으로 렌더링한다.

**Tech Stack:** Figma Plugin API 1.0.0, Claude API (claude-sonnet-4-6 vision), Vanilla JS (no bundler)

---

## 파일 구조

```
Figma-Clean-Plugin/
└── utbot/
    ├── manifest.json   ← 플러그인 메타 + networkAccess
    ├── ui.html         ← 패널 UI (API 키 관리, 분석 실행, 상태 표시)
    └── code.js         ← 전체 로직 (규칙 스캔 + Claude 호출 + 리포트 빌드)
```

`code.js` 내부 섹션 구조:
```
[1] 유틸 함수 (색상 대비, 그리드 정렬)
[2] 규칙 스캐너 (scanRules)
[3] Claude API 호출 (callClaude)
[4] 리포트 프레임 빌더 (buildReportFrame)
[5] 메시지 핸들러 + 진입점
```

---

## Task 1: manifest.json 생성

**Files:**
- Create: `utbot/manifest.json`

- [ ] **Step 1: utbot 폴더 생성 후 manifest.json 작성**

```json
{
  "name": "utbot",
  "id": "utbot-usability-tester-001",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": ["figma"],
  "networkAccess": {
    "allowedDomains": ["https://api.anthropic.com"]
  }
}
```

- [ ] **Step 2: 파일 저장 확인**

Figma Desktop → Plugins → Development → Import plugin from manifest → `utbot/manifest.json` 선택.  
오류 없이 "utbot" 플러그인이 목록에 나타나면 통과.

- [ ] **Step 3: 커밋**

```bash
git add utbot/manifest.json
git commit -m "feat: add utbot manifest"
```

---

## Task 2: ui.html — 패널 UI 전체

**Files:**
- Create: `utbot/ui.html`

ui.html은 4가지 상태를 가진다: `key-input` → `ready` → `analyzing` → `done` / `error`

- [ ] **Step 1: ui.html 작성**

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8" />
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; font-family: -apple-system, sans-serif; }
  body { background: #1e1e1e; color: #fff; padding: 16px; width: 280px; }
  h1 { font-size: 16px; font-weight: 700; margin-bottom: 16px; color: #fff; }
  .section { display: none; }
  .section.active { display: block; }

  label { font-size: 11px; color: #aaa; display: block; margin-bottom: 6px; }
  input[type="password"], input[type="text"] {
    width: 100%; padding: 8px 10px; border-radius: 6px;
    border: 1px solid #444; background: #2c2c2c; color: #fff;
    font-size: 13px; margin-bottom: 10px;
  }
  button {
    width: 100%; padding: 9px; border-radius: 6px; border: none;
    background: #18a0fb; color: #fff; font-size: 13px;
    font-weight: 600; cursor: pointer;
  }
  button:hover { background: #0d8de3; }
  button.secondary {
    background: #3c3c3c; color: #ccc; margin-top: 8px;
  }
  button.secondary:hover { background: #4a4a4a; }

  .frame-name {
    font-size: 13px; color: #ccc; background: #2c2c2c;
    border-radius: 6px; padding: 8px 10px; margin-bottom: 14px;
    border: 1px solid #444; word-break: break-all;
  }
  .no-frame { font-size: 12px; color: #888; margin-bottom: 14px; }

  .steps { margin-bottom: 16px; }
  .step { font-size: 12px; color: #888; padding: 4px 0; display: flex; align-items: center; gap: 8px; }
  .step.active { color: #fff; }
  .step.done { color: #4caf50; }
  .step-dot { width: 8px; height: 8px; border-radius: 50%; background: #444; flex-shrink: 0; }
  .step.active .step-dot { background: #18a0fb; }
  .step.done .step-dot { background: #4caf50; }

  .error-msg { font-size: 12px; color: #f44; background: #2a1a1a;
    border-radius: 6px; padding: 10px; margin-bottom: 12px; line-height: 1.5; }
  .success-msg { font-size: 13px; color: #4caf50; text-align: center;
    margin-bottom: 12px; }
</style>
</head>
<body>
<h1>utbot</h1>

<!-- 상태 1: API 키 입력 -->
<div id="s-key" class="section active">
  <label>Claude API Key</label>
  <input type="password" id="api-key-input" placeholder="sk-ant-..." />
  <button id="btn-save-key">저장</button>
</div>

<!-- 상태 2: 분석 대기 -->
<div id="s-ready" class="section">
  <label>선택된 프레임</label>
  <div id="frame-name-display" class="frame-name">—</div>
  <button id="btn-analyze">분석 시작</button>
  <button id="btn-change-key" class="secondary">API 키 변경</button>
</div>

<!-- 상태 3: 분석 중 -->
<div id="s-analyzing" class="section">
  <div class="steps">
    <div class="step" id="step-rules"><div class="step-dot"></div>규칙 기반 스캔</div>
    <div class="step" id="step-capture"><div class="step-dot"></div>프레임 캡처</div>
    <div class="step" id="step-claude"><div class="step-dot"></div>Claude 분석 중</div>
    <div class="step" id="step-build"><div class="step-dot"></div>리포트 생성 중</div>
  </div>
</div>

<!-- 상태 4a: 완료 -->
<div id="s-done" class="section">
  <div class="success-msg">✅ 분석 완료!</div>
  <button id="btn-again">다시 분석</button>
</div>

<!-- 상태 4b: 오류 -->
<div id="s-error" class="section">
  <div id="error-text" class="error-msg"></div>
  <button id="btn-retry">다시 시도</button>
  <button id="btn-change-key-err" class="secondary">API 키 변경</button>
</div>

<script>
  // ── 상태 전환 ──
  function show(id) {
    document.querySelectorAll('.section').forEach(el => el.classList.remove('active'));
    document.getElementById(id).classList.add('active');
  }

  function setStep(stepId, state) { // state: 'active' | 'done'
    const el = document.getElementById(stepId);
    el.classList.remove('active', 'done');
    if (state) el.classList.add(state);
  }

  // ── 버튼 이벤트 ──
  document.getElementById('btn-save-key').onclick = () => {
    const key = document.getElementById('api-key-input').value.trim();
    if (!key.startsWith('sk-ant-')) {
      alert('올바른 Claude API 키를 입력해주세요 (sk-ant- 로 시작)');
      return;
    }
    parent.postMessage({ pluginMessage: { type: 'save-key', key } }, '*');
  };

  document.getElementById('btn-analyze').onclick = () => {
    parent.postMessage({ pluginMessage: { type: 'analyze' } }, '*');
  };

  document.getElementById('btn-change-key').onclick = () => show('s-key');
  document.getElementById('btn-change-key-err').onclick = () => show('s-key');

  document.getElementById('btn-again').onclick = () => {
    ['step-rules','step-capture','step-claude','step-build'].forEach(s => setStep(s, null));
    show('s-ready');
  };

  document.getElementById('btn-retry').onclick = () => {
    ['step-rules','step-capture','step-claude','step-build'].forEach(s => setStep(s, null));
    parent.postMessage({ pluginMessage: { type: 'analyze' } }, '*');
    show('s-analyzing');
  };

  // ── code.js 메시지 수신 ──
  window.onmessage = (event) => {
    const msg = event.data.pluginMessage;
    if (!msg) return;

    if (msg.type === 'init') {
      if (msg.hasKey) {
        document.getElementById('frame-name-display').textContent =
          msg.frameName || '프레임을 선택해주세요';
        show('s-ready');
      } else {
        show('s-key');
      }
    }

    if (msg.type === 'key-saved') {
      document.getElementById('frame-name-display').textContent =
        msg.frameName || '프레임을 선택해주세요';
      show('s-ready');
    }

    if (msg.type === 'selection-change') {
      document.getElementById('frame-name-display').textContent =
        msg.frameName || '프레임을 선택해주세요';
    }

    if (msg.type === 'progress') {
      show('s-analyzing');
      const stepMap = {
        rules: 'step-rules',
        capture: 'step-capture',
        claude: 'step-claude',
        build: 'step-build',
      };
      // 이전 스텝 done 처리
      const order = ['rules', 'capture', 'claude', 'build'];
      const idx = order.indexOf(msg.step);
      order.slice(0, idx).forEach(s => setStep(stepMap[s], 'done'));
      setStep(stepMap[msg.step], 'active');
    }

    if (msg.type === 'done') show('s-done');

    if (msg.type === 'error') {
      document.getElementById('error-text').textContent = msg.message;
      show('s-error');
    }
  };
</script>
</body>
</html>
```

- [ ] **Step 2: Figma에서 UI 확인**

Figma → 플러그인 실행 → API 키 입력 화면이 나타나는지 확인. (code.js가 없으면 오류 나도 무방, UI 구조만 확인)

- [ ] **Step 3: 커밋**

```bash
git add utbot/ui.html
git commit -m "feat: add utbot panel UI with 4 states"
```

---

## Task 3: code.js — 유틸 함수 + 초기화

**Files:**
- Create: `utbot/code.js`

- [ ] **Step 1: code.js 뼈대 + 유틸 함수 작성**

```javascript
// ══════════════════════════════════════════════════════════════════
// [1] 유틸 함수
// ══════════════════════════════════════════════════════════════════

/** sRGB 상대 휘도 (Figma 0-1 기준) */
function relativeLuminance(r, g, b) {
  const toLinear = c => c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4);
  return 0.2126 * toLinear(r) + 0.7152 * toLinear(g) + 0.0722 * toLinear(b);
}

/** WCAG 대비율 */
function contrastRatio(c1, c2) {
  const l1 = relativeLuminance(c1.r, c1.g, c1.b);
  const l2 = relativeLuminance(c2.r, c2.g, c2.b);
  const lighter = Math.max(l1, l2);
  const darker  = Math.min(l1, l2);
  return (lighter + 0.05) / (darker + 0.05);
}

/** Figma fill에서 solid 색상 추출. 없으면 null */
function getSolidFill(fills) {
  if (!fills || fills.length === 0) return null;
  const solid = fills.find(f => f.type === 'SOLID' && f.visible !== false);
  return solid ? solid.color : null;
}

/** 노드 트리 전체 순회 (DFS) */
function* walkTree(node) {
  yield node;
  if ('children' in node) {
    for (const child of node.children) yield* walkTree(child);
  }
}

/** 4px 그리드 정렬 여부 */
function isGridAligned(val) {
  return Math.round(val) % 4 === 0;
}
```

- [ ] **Step 2: 초기화 + 메시지 핸들러 스켈레톤 추가**

```javascript
// ══════════════════════════════════════════════════════════════════
// [5] 메시지 핸들러 + 진입점
// ══════════════════════════════════════════════════════════════════

figma.showUI(__html__, { width: 280, height: 320 });

// 선택된 프레임 이름 반환
function getSelectedFrameName() {
  const sel = figma.currentPage.selection;
  if (sel.length === 1 && (sel[0].type === 'FRAME' || sel[0].type === 'COMPONENT')) {
    return sel[0].name;
  }
  return null;
}

// 초기화: API 키 확인 후 UI에 알림
async function init() {
  const key = await figma.clientStorage.getAsync('utbot-api-key');
  figma.ui.postMessage({
    type: 'init',
    hasKey: !!key,
    frameName: getSelectedFrameName(),
  });
}
init();

// 선택 변경 감지
figma.on('selectionchange', () => {
  figma.ui.postMessage({
    type: 'selection-change',
    frameName: getSelectedFrameName(),
  });
});

// UI 메시지 수신
figma.ui.onmessage = async (msg) => {
  if (msg.type === 'save-key') {
    await figma.clientStorage.setAsync('utbot-api-key', msg.key);
    figma.ui.postMessage({
      type: 'key-saved',
      frameName: getSelectedFrameName(),
    });
    return;
  }

  if (msg.type === 'analyze') {
    await runAnalysis();
  }
};

async function runAnalysis() {
  // 구현은 Task 4~6에서 추가
  figma.ui.postMessage({ type: 'error', message: '아직 구현 중입니다.' });
}
```

- [ ] **Step 3: Figma에서 동작 확인**

플러그인 실행 → API 키 입력 화면 표시 → 키 저장 → 분석 대기 화면으로 전환 확인.  
프레임 선택/해제 시 선택된 프레임 이름이 패널에서 바뀌는지 확인.

- [ ] **Step 4: 커밋**

```bash
git add utbot/code.js
git commit -m "feat: add utbot code.js skeleton with init and key management"
```

---

## Task 4: code.js — 규칙 기반 스캐너

**Files:**
- Modify: `utbot/code.js` (유틸 함수 섹션 아래에 추가)

- [ ] **Step 1: scanRules 함수 추가**

유틸 함수 블록 바로 아래(`[5] 메시지 핸들러` 위)에 다음 코드를 삽입:

```javascript
// ══════════════════════════════════════════════════════════════════
// [2] 규칙 기반 스캐너
// ══════════════════════════════════════════════════════════════════

/**
 * 프레임 노드를 순회하며 7가지 규칙을 체크.
 * @returns {Array<{rule, severity, message, nodeId}>}
 *   severity: 'fail' | 'warn' | 'pass'
 */
function scanRules(frameNode) {
  const findings = [];
  const warn = (rule, message, nodeId) =>
    findings.push({ rule, severity: 'warn', message, nodeId: nodeId || null });
  const fail = (rule, message, nodeId) =>
    findings.push({ rule, severity: 'fail', message, nodeId: nodeId || null });

  // 수집용
  const fontSizes = new Set();
  let totalNodes = 0;
  let misalignedCount = 0;
  const componentMap = {}; // componentId → [{ w, h }]

  for (const node of walkTree(frameNode)) {
    if (node.id === frameNode.id) continue; // 루트 제외
    totalNodes++;

    // ── 규칙 1: 소형 텍스트 (fontSize < 12px) ──────────────────────
    if (node.type === 'TEXT') {
      const size = typeof node.fontSize === 'number' ? node.fontSize : 12;
      fontSizes.add(size);
      if (size < 12) {
        warn('small-text', `텍스트 "${node.characters?.slice(0, 20)}" 크기 ${size}px (12px 미만)`, node.id);
      }

      // ── 규칙 2: 색상 대비 ──────────────────────────────────────────
      const textColor = getSolidFill(node.fills);
      if (textColor && node.parent) {
        const bgColor = getSolidFill(node.parent.fills);
        if (bgColor) {
          const ratio = contrastRatio(textColor, bgColor);
          if (ratio < 4.5) {
            fail('contrast',
              `텍스트 "${node.characters?.slice(0, 20)}" 대비율 ${ratio.toFixed(1)}:1 (WCAG AA 4.5:1 미만)`,
              node.id);
          }
        }
      }
    }

    // ── 규칙 3: 터치 타겟 크기 (< 44×44px) ─────────────────────────
    if (node.type === 'INSTANCE' || node.type === 'COMPONENT') {
      const name = node.name.toLowerCase();
      const isInteractive = name.includes('button') || name.includes('btn') ||
        name.includes('tab') || name.includes('chip') || name.includes('icon');
      if (isInteractive) {
        const w = 'width' in node ? node.width : 0;
        const h = 'height' in node ? node.height : 0;
        if (w < 44 || h < 44) {
          warn('touch-target',
            `"${node.name}" 터치 타겟 ${Math.round(w)}×${Math.round(h)}px (44px 미만)`,
            node.id);
        }
      }
    }

    // ── 규칙 4: 아이콘 전용 버튼 (TEXT 자식 없음) ──────────────────
    if (node.type === 'INSTANCE') {
      const name = node.name.toLowerCase();
      const isBtn = name.includes('button') || name.includes('btn') || name.includes('icon-btn');
      if (isBtn) {
        const hasText = 'children' in node &&
          [...walkTree(node)].some(n => n.id !== node.id && n.type === 'TEXT' && n.characters?.trim());
        if (!hasText) {
          warn('icon-only-button',
            `"${node.name}" 텍스트 레이블 없는 버튼`,
            node.id);
        }
      }
    }

    // ── 규칙 5: 텍스트 계층 구조 (폰트 크기 4가지 초과) ────────────
    // (루프 끝에서 체크)

    // ── 규칙 6: 여백 일관성 (4px 그리드 벗어남) ────────────────────
    if ('x' in node && 'y' in node && node.type !== 'TEXT') {
      if (!isGridAligned(node.x) || !isGridAligned(node.y)) {
        misalignedCount++;
      }
    }

    // ── 규칙 7: 컴포넌트 일관성 ────────────────────────────────────
    if (node.type === 'INSTANCE' && node.mainComponent) {
      const cid = node.mainComponent.id;
      if (!componentMap[cid]) componentMap[cid] = [];
      componentMap[cid].push({
        w: Math.round(node.width),
        h: Math.round(node.height),
        name: node.name,
        id: node.id,
      });
    }
  }

  // ── 규칙 5 결과 집계 ──────────────────────────────────────────────
  if (fontSizes.size > 4) {
    warn('text-hierarchy',
      `폰트 크기 ${fontSizes.size}가지 사용 (4가지 초과 — 텍스트 계층 불명확)`);
  }

  // ── 규칙 6 결과 집계 ──────────────────────────────────────────────
  if (totalNodes > 0) {
    const misalignedRatio = misalignedCount / totalNodes;
    if (misalignedRatio > 0.1) {
      warn('grid-alignment',
        `전체 노드 중 ${Math.round(misalignedRatio * 100)}%가 4px 그리드 미정렬`);
    }
  }

  // ── 규칙 7 결과 집계 ──────────────────────────────────────────────
  for (const [, instances] of Object.entries(componentMap)) {
    if (instances.length < 2) continue;
    const sizes = new Set(instances.map(i => `${i.w}x${i.h}`));
    if (sizes.size > 1) {
      warn('component-consistency',
        `"${instances[0].name}" 컴포넌트가 ${sizes.size}가지 다른 크기로 사용됨 (${[...sizes].join(', ')})`);
    }
  }

  return findings;
}
```

- [ ] **Step 2: runAnalysis에서 규칙 스캔 호출 + 진행 상태 전송**

`runAnalysis` 함수를 다음으로 교체:

```javascript
async function runAnalysis() {
  const sel = figma.currentPage.selection;
  if (sel.length !== 1 || (sel[0].type !== 'FRAME' && sel[0].type !== 'COMPONENT')) {
    figma.ui.postMessage({ type: 'error', message: '분석할 프레임을 하나만 선택해주세요.' });
    return;
  }

  const frame = sel[0];
  const apiKey = await figma.clientStorage.getAsync('utbot-api-key');
  if (!apiKey) {
    figma.ui.postMessage({ type: 'error', message: 'API 키가 없습니다. 키를 먼저 저장해주세요.' });
    return;
  }

  try {
    // Step: 규칙 스캔
    figma.ui.postMessage({ type: 'progress', step: 'rules' });
    const findings = scanRules(frame);

    // Step: 캡처 (Task 5에서 구현)
    figma.ui.postMessage({ type: 'progress', step: 'capture' });
    const uint8 = await frame.exportAsync({ format: 'PNG', constraint: { type: 'SCALE', value: 1 } });
    let binary = '';
    for (let i = 0; i < uint8.length; i++) binary += String.fromCharCode(uint8[i]);
    const base64 = btoa(binary);

    // Step: Claude 호출 (Task 5에서 구현)
    figma.ui.postMessage({ type: 'progress', step: 'claude' });
    const analysis = await callClaude(apiKey, base64, findings, frame.name);

    // Step: 리포트 프레임 생성 (Task 6에서 구현)
    figma.ui.postMessage({ type: 'progress', step: 'build' });
    buildReportFrame(frame, findings, analysis);

    figma.ui.postMessage({ type: 'done' });
  } catch (e) {
    figma.ui.postMessage({ type: 'error', message: e.message || '알 수 없는 오류가 발생했습니다.' });
  }
}
```

- [ ] **Step 3: Figma에서 규칙 스캔 확인**

프레임 선택 → 분석 시작 → progress `rules` 메시지가 전달되어 분석 중 화면으로 전환되는지 확인.  
(callClaude / buildReportFrame 미구현으로 error 상태까지 가는 건 정상)

- [ ] **Step 4: 커밋**

```bash
git add utbot/code.js
git commit -m "feat: add rule-based scanner with 7 heuristic checks"
```

---

## Task 5: code.js — Claude API 호출

**Files:**
- Modify: `utbot/code.js` (규칙 스캐너 섹션 아래에 추가)

- [ ] **Step 1: callClaude 함수 추가**

`[2] 규칙 기반 스캐너` 블록 아래에 삽입:

```javascript
// ══════════════════════════════════════════════════════════════════
// [3] Claude API 호출
// ══════════════════════════════════════════════════════════════════

/**
 * 프레임 이미지 + 규칙 결과를 Claude에 보내 사용성 분석 JSON을 받는다.
 * @returns {{insights, 기능인지, 과업흐름, 탐색유도, 오류방지, 개선아이디어, scores, 총평}}
 */
async function callClaude(apiKey, base64png, findings, frameName) {
  const failCount = findings.filter(f => f.severity === 'fail').length;
  const warnCount = findings.filter(f => f.severity === 'warn').length;
  const findingsSummary = findings.map(f =>
    `[${f.severity === 'fail' ? '❌ 실패' : '⚠️ 경고'}] ${f.rule}: ${f.message}`
  ).join('\n') || '자동 감지된 문제 없음';

  const prompt = `당신은 UX 전문가입니다. 아래 Figma 프레임 "${frameName}"의 스크린샷과 자동 규칙 검사 결과를 바탕으로 Nielsen Norman Group 10가지 사용성 휴리스틱 기준으로 분석하세요.

[자동 규칙 검사 결과]
실패: ${failCount}건, 경고: ${warnCount}건
${findingsSummary}

반드시 아래 JSON 형식으로만 응답하세요. JSON 외 다른 텍스트는 절대 포함하지 마세요.

{
  "insights": [
    { "content": "인사이트 내용 (한 문장)", "description": "상세 설명", "heuristic": "적용 Nielsen 휴리스틱 이름" }
  ],
  "기능인지": "사용자가 주요 기능을 얼마나 쉽게 인지하는지 2-3문장 분석",
  "과업흐름": "목표 달성까지의 흐름 자연스러움 분석",
  "탐색유도": "내비게이션, 메뉴 구조, 탐색 경험 분석",
  "오류방지": "입력 오류 방지, 에러 메시지 품질 분석",
  "개선아이디어": "핵심 개선 제안 1-2가지",
  "scores": {
    "기능인지": 3,
    "과업흐름": 3,
    "탐색유도": 3,
    "오류방지": 3,
    "전체사용성": 3
  },
  "총평": "전체 사용성 총평 2-3문장"
}`;

  const res = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: {
      'x-api-key': apiKey,
      'anthropic-version': '2023-06-01',
      'content-type': 'application/json',
    },
    body: JSON.stringify({
      model: 'claude-sonnet-4-6',
      max_tokens: 2048,
      messages: [{
        role: 'user',
        content: [
          {
            type: 'image',
            source: { type: 'base64', media_type: 'image/png', data: base64png },
          },
          { type: 'text', text: prompt },
        ],
      }],
    }),
  });

  if (!res.ok) {
    const errBody = await res.text();
    if (res.status === 401) throw new Error('API 키가 올바르지 않습니다.');
    if (res.status === 429) throw new Error('API 요청 한도 초과. 잠시 후 다시 시도해주세요.');
    throw new Error(`Claude API 오류 (${res.status}): ${errBody.slice(0, 100)}`);
  }

  const data = await res.json();
  const text = data.content?.[0]?.text || '';

  // JSON 파싱 (마크다운 코드블록 제거)
  const jsonMatch = text.match(/\{[\s\S]*\}/);
  if (!jsonMatch) throw new Error('Claude 응답을 파싱할 수 없습니다.');
  return JSON.parse(jsonMatch[0]);
}
```

- [ ] **Step 2: Figma에서 Claude 호출 확인**

유효한 Claude API 키로 프레임 분석 실행 → progress `claude` 단계가 활성화되고, JSON 응답을 받는지 확인.  
(`buildReportFrame` 미구현이므로 `step-build` 이후 오류 발생은 정상)

- [ ] **Step 3: 커밋**

```bash
git add utbot/code.js
git commit -m "feat: add Claude API caller with vision + rule context"
```

---

## Task 6: code.js — 리포트 프레임 빌더

**Files:**
- Modify: `utbot/code.js` (Claude 호출 섹션 아래에 추가)

- [ ] **Step 1: buildReportFrame 함수 추가**

`[3] Claude API 호출` 블록 아래에 삽입:

```javascript
// ══════════════════════════════════════════════════════════════════
// [4] 리포트 프레임 빌더
// ══════════════════════════════════════════════════════════════════

const REPORT_WIDTH = 600;
const COLORS = {
  bg:       { r: 1,    g: 1,    b: 1    },
  title:    { r: 0.07, g: 0.07, b: 0.07 },
  body:     { r: 0.2,  g: 0.2,  b: 0.2  },
  muted:    { r: 0.5,  g: 0.5,  b: 0.5  },
  accent:   { r: 0.09, g: 0.63, b: 0.98 },
  fail:     { r: 0.96, g: 0.26, b: 0.21 },
  warn:     { r: 1,    g: 0.76, b: 0    },
  pass:     { r: 0.3,  g: 0.69, b: 0.31 },
  divider:  { r: 0.9,  g: 0.9,  b: 0.9  },
  sectionBg:{ r: 0.97, g: 0.97, b: 0.97 },
};

async function buildReportFrame(sourceFrame, findings, analysis) {
  // 폰트 로드
  await figma.loadFontAsync({ family: 'Inter', style: 'Bold' });
  await figma.loadFontAsync({ family: 'Inter', style: 'Regular' });

  // 루트 프레임 (VERTICAL 오토레이아웃)
  const report = figma.createFrame();
  report.name = `utbot — ${sourceFrame.name}`;
  report.x = Math.round(sourceFrame.x + sourceFrame.width + 40);
  report.y = sourceFrame.y;
  report.layoutMode = 'VERTICAL';
  report.primaryAxisSizingMode = 'AUTO';
  report.counterAxisSizingMode = 'FIXED';
  report.resize(REPORT_WIDTH, 100); // 높이는 AUTO
  report.paddingTop = 40;
  report.paddingBottom = 40;
  report.paddingLeft = 40;
  report.paddingRight = 40;
  report.itemSpacing = 0;
  report.fills = [{ type: 'SOLID', color: COLORS.bg }];
  report.cornerRadius = 12;

  const addText = (parent, text, size, weight, color, opts = {}) => {
    const t = figma.createText();
    parent.appendChild(t);
    t.fontName = { family: 'Inter', style: weight };
    t.fontSize = size;
    t.characters = text || ' ';
    t.fills = [{ type: 'SOLID', color }];
    t.textAutoResize = 'WIDTH_AND_HEIGHT';
    if (opts.width) {
      t.textAutoResize = 'HEIGHT';
      t.resize(opts.width, 20);
    }
    if (opts.opacity) t.opacity = opts.opacity;
    t.layoutSizingHorizontal = opts.fill ? 'FILL' : 'HUG';
    return t;
  };

  const addSpacer = (parent, height) => {
    const f = figma.createFrame();
    parent.appendChild(f);
    f.resize(REPORT_WIDTH - 80, Math.max(1, height));
    f.fills = [];
    f.layoutSizingHorizontal = 'FILL';
    return f;
  };

  const addDivider = (parent) => {
    const d = figma.createFrame();
    parent.appendChild(d);
    d.resize(REPORT_WIDTH - 80, 1);
    d.fills = [{ type: 'SOLID', color: COLORS.divider }];
    d.layoutSizingHorizontal = 'FILL';
    return d;
  };

  const addSection = (parent, title) => {
    addSpacer(parent, 28);
    addText(parent, title, 13, 'Bold', COLORS.accent, { fill: true });
    addSpacer(parent, 10);
  };

  const scoreBar = (parent, label, score) => {
    const row = figma.createFrame();
    parent.appendChild(row);
    row.layoutMode = 'HORIZONTAL';
    row.primaryAxisSizingMode = 'FIXED';
    row.counterAxisSizingMode = 'AUTO';
    row.resize(REPORT_WIDTH - 80, 20);
    row.fills = [];
    row.itemSpacing = 12;
    row.layoutSizingHorizontal = 'FILL';

    const lbl = figma.createText();
    row.appendChild(lbl);
    lbl.fontName = { family: 'Inter', style: 'Regular' };
    lbl.fontSize = 13;
    lbl.characters = label;
    lbl.fills = [{ type: 'SOLID', color: COLORS.body }];
    lbl.textAutoResize = 'WIDTH_AND_HEIGHT';
    lbl.layoutSizingHorizontal = 'FILL';

    const stars = '★'.repeat(score) + '☆'.repeat(5 - score);
    const starsT = figma.createText();
    row.appendChild(starsT);
    starsT.fontName = { family: 'Inter', style: 'Regular' };
    starsT.fontSize = 13;
    starsT.characters = `${stars} (${score}/5)`;
    starsT.fills = [{ type: 'SOLID', color: score >= 4 ? COLORS.pass : score >= 3 ? COLORS.warn : COLORS.fail }];
    starsT.textAutoResize = 'WIDTH_AND_HEIGHT';

    addSpacer(parent, 8);
  };

  // ── 헤더 ──────────────────────────────────────────────────────────
  addText(report, 'utbot 사용성 분석 리포트', 22, 'Bold', COLORS.title, { fill: true });
  addSpacer(report, 6);
  const date = new Date().toISOString().slice(0, 10);
  addText(report, `${sourceFrame.name}  ·  ${date}`, 12, 'Regular', COLORS.muted, { fill: true });

  // ── 규칙 기반 체크 ──────────────────────────────────────────────
  addSection(report, '📋 규칙 기반 체크');
  addDivider(report);
  addSpacer(report, 12);

  if (findings.length === 0) {
    addText(report, '✅ 자동 감지된 문제 없음', 13, 'Regular', COLORS.pass, { fill: true });
  } else {
    for (const f of findings) {
      const icon = f.severity === 'fail' ? '❌' : '⚠️';
      addText(report, `${icon}  ${f.message}`, 12, 'Regular', COLORS.body, { fill: true });
      addSpacer(report, 6);
    }
  }

  // ── 핵심 인사이트 ─────────────────────────────────────────────────
  addSection(report, '💡 핵심 인사이트');
  addDivider(report);
  addSpacer(report, 12);

  if (analysis.insights?.length) {
    for (const ins of analysis.insights) {
      addText(report, `• ${ins.content}`, 13, 'Bold', COLORS.title, { fill: true });
      addSpacer(report, 4);
      addText(report, ins.description, 12, 'Regular', COLORS.body, { fill: true });
      addSpacer(report, 2);
      addText(report, `→ ${ins.heuristic}`, 11, 'Regular', COLORS.accent, { fill: true });
      addSpacer(report, 12);
    }
  }

  // ── 분석 섹션 ────────────────────────────────────────────────────
  const analysisSections = [
    ['🔍 기능 인지', analysis['기능인지']],
    ['🔄 과업 흐름', analysis['과업흐름']],
    ['🧭 탐색 유도', analysis['탐색유도']],
    ['🛡️ 오류 방지', analysis['오류방지']],
  ];
  for (const [title, content] of analysisSections) {
    addSection(report, title);
    addDivider(report);
    addSpacer(report, 10);
    addText(report, content || '—', 13, 'Regular', COLORS.body, { fill: true });
  }

  if (analysis['개선아이디어']) {
    addSection(report, '✏️ 개선 아이디어');
    addDivider(report);
    addSpacer(report, 10);
    addText(report, analysis['개선아이디어'], 13, 'Regular', COLORS.body, { fill: true });
  }

  // ── 종합 평가 ────────────────────────────────────────────────────
  addSection(report, '📊 종합 평가');
  addDivider(report);
  addSpacer(report, 12);

  const scores = analysis.scores || {};
  const scoreKeys = [
    ['기능 인지', '기능인지'],
    ['과업 흐름', '과업흐름'],
    ['탐색 유도', '탐색유도'],
    ['오류 방지', '오류방지'],
    ['전체 사용성', '전체사용성'],
  ];
  for (const [label, key] of scoreKeys) {
    scoreBar(report, label, Math.min(5, Math.max(1, scores[key] || 3)));
  }

  addSection(report, '📝 총평');
  addDivider(report);
  addSpacer(report, 10);
  addText(report, analysis['총평'] || '—', 13, 'Regular', COLORS.body, { fill: true });

  // 캔버스에 추가 + 포커스
  figma.currentPage.appendChild(report);
  figma.currentPage.selection = [report];
  figma.viewport.scrollAndZoomIntoView([report]);
}
```

- [ ] **Step 2: Figma에서 전체 플로우 확인**

프레임 선택 → 분석 시작 → 4단계 진행 상태 → 선택 프레임 오른쪽에 리포트 프레임 생성 확인.  
헤더 / 규칙 체크 / 인사이트 / 분석 섹션 / 종합 평가 모두 정상 출력되는지 확인.

- [ ] **Step 3: 커밋**

```bash
git add utbot/code.js
git commit -m "feat: add report frame builder with full usability report layout"
```

---

## Task 7: 최종 통합 테스트 + 푸시

**Files:**
- Modify: `utbot/manifest.json` (id 최종 확인)

- [ ] **Step 1: 다양한 프레임으로 테스트**

아래 케이스를 직접 확인:
1. 텍스트만 있는 단순 프레임 → 규칙 경고 0건, Claude 분석 정상 출력
2. 버튼 컴포넌트가 포함된 프레임 → 터치 타겟 / 아이콘 버튼 규칙 동작 확인
3. 프레임 미선택 상태에서 분석 시작 → "프레임을 하나만 선택해주세요" 오류 메시지 확인
4. 잘못된 API 키 → "API 키가 올바르지 않습니다" 오류 메시지 확인

- [ ] **Step 2: Git push**

```bash
git add utbot/
git commit -m "feat: complete utbot usability test plugin"
git push
```

- [ ] **Step 3: 완료 확인**

GitHub `yuz-w/Figma-parking-plugin` 레포에 `utbot/` 폴더(manifest.json, code.js, ui.html) 3개 파일 업로드 확인.
